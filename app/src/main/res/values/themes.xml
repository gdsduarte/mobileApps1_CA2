<resources>
    <!-- Base application theme. -->
    <style name="Theme.MoveBookingApp" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/teal_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorOnSecondary</item>
        <!-- Customize your theme here. -->
    </style>

    <!--  Add a custom font to the theme. -->
    <style name="Theme.MoveBookingApp.RobotoCondensed" parent="Theme.MoveBookingApp">
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>

    <!--  Add a custom shape to the button. -->
    <style name="Shape">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">5dp</item>
    </style>
</resources>
